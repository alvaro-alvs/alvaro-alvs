

export type ForYouContentType = {
    title?: string,
    subtitle?: string,
    items?: string[],
    icon?: React.ReactNode,
    image?: React.ReactNode,
    link?: string
}

export type ContactType = {
    nome: string, 
    email: string,
    telefone: string,
    message: string,
    product: string
}